import React from "react";
import styles from "../Styles/Components.scss";

function IconButton() {
    return (
        <button id="icon-button" /* type="button" onClick={handleClick}*/>
            <img></img> 
        </button>
      );
}

export default IconButton;